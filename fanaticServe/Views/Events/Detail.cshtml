@model fanaticServe.Core.Dto.DetailEvent

@{
    ViewData["Title"] = Model.Title;
}
<div class="row">
    <div>
        <h1>@Html.DisplayFor(modelItem => Model.Title)</h1>
    </div>
</div>
<div class="card">
    <div class="card-body">
        @await Html.PartialAsync("_DetailEventView", Model)
    </div>
</div>